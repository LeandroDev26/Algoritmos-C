#include <stdio.h>
#include <stdlib.h>

int main()
{
    /*
    A implementação da função de busca é exatamente a mesma para uma Árvore Binária de Busca (ABB) e para uma Árvore AVL.
A busca em uma árvore binária, seja ela uma ABB comum ou uma AVL, segue uma única regra fundamental:

Comece pela raiz.
Se o valor procurado for menor que o do nó atual, vá para a esquerda.
Se o valor procurado for maior que o do nó atual, vá para a direita.
Se for igual, você encontrou o valor.
Se chegar a um galho nulo (vazio), o valor não existe na árvore.

Uma Árvore AVL é uma Árvore Binária de Busca. Ela respeita essa regra perfeitamente. A função de busca apenas lê os dados
da árvore para encontrar um caminho; ela não modifica a estrutura, não verifica alturas nem se preocupa com o fator de
balanceamento.

    */
}
