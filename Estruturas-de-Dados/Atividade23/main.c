#include <stdio.h>
#include <stdlib.h>

int main()
{
   /*
   A relação entre a altura h e o número de vértices (ou nós) n em uma
   árvore binária completa é expressa pela seguinte equação:

     h = [log2 n]
   */
}
