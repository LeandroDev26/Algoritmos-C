#include <stdio.h>
#include <stdlib.h>

int main()
{
    /*
      Uma árvore AVL é um tipo especial de Árvore Binária de Busca (ABB) que se auto-balanceia.
    Que torna uma árvore uma AVL é uma regra muito estrita: para qualquer nó na árvore, a diferença
    entre a altura da sua subárvore direita e a altura da sua subárvore esquerda não pode ser maior que 1.

    Essa diferença é chamada de Fator de Balanceamento, e para todo nó em uma AVL, ele deve ser -1, 0 ou 1.

    Fator -1: A subárvore esquerda é um nível mais alta que a direita.
    Fator 0: As duas subárvores têm a mesma altura.
    Fator 1: A subárvore direita é um nível mais alta que a esquerda.
    Essa regra garante que a altura da árvore seja sempre logarítmica (O(log n)), o que evita o pior caso de
     uma ABB, que é se tornar uma longa lista ligada.

    */
}
