#include <stdio.h>
#include <stdlib.h>

int main()
{
    /*
    Uma Árvore Binária de Busca (ABB) é usada para pesquisar dados rapidamente. Seu ponto
     fraco é que, seos dados forem inseridos em ordem, ela se torna lenta como uma lista.
    Para resolver isso, existem as árvores balanceadas (como a AVL), que se autoajustam
    com rotações para garantir que a busca seja sempre rápida (O(log n)), ao custo de serem
     mais complexas de programar.

    */
}
