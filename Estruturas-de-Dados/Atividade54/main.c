#include <stdio.h>
#include <stdlib.h>

int main()
{
    /*
    A sequência que não pode ser o resultado de uma busca pelo nó 363 é a (c).

    Explicação da Regra
    Para encontrar um valor em uma Árvore Binária de Busca (ABB), seguimos uma regra simples:

    Se o valor procurado é menor que o nó atual, a busca continua obrigatoriamente pela subárvore esquerda.
    Se o valor procurado é maior que o nó atual, a busca continua obrigatoriamente pela subárvore direita.
    Isso significa que, a cada passo, o intervalo de valores possíveis para os próximos nós visitados vai se afunilando.
     Um caminho de busca só é válido se obedecer a essa regra em todas as etapas.

    */
}
